/* eslint-disable no-unused-vars */
import React from 'react';

const FirstPage = () => {
    return (
        <div>
            <p className='text-center'>We are in home page now.</p>
        </div>
    );
};

export default FirstPage;